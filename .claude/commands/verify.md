---
description: 커밋 전 전체 검증 — typecheck, 테스트, 그리고 실제 실행 관찰
---

`PROMPT.md` §2.5의 개발 루프를 완주한다. 순서대로:

1. `pnpm verify` (typecheck + 단위 테스트)
2. **실제 실행 관찰** — 단위 테스트 통과는 관찰이 아니다:
   - 대시보드를 건드렸다면 → Playwright로 띄워 스크린샷을 찍고 **이미지를 직접 확인**한다.
     데스크톱과 모바일 뷰포트 모두.
   - 워커를 건드렸다면 → 픽스처를 주입해 3틱 돌리고 상태 전이 로그를 확인한다
   - 알림을 건드렸다면 → 실제 테스트 메일을 보내고 수신을 확인한다
   - 예약 흐름을 건드렸다면 → `/dryrun`으로 페이로드를 검증한다
3. 자기 diff를 적대적으로 다시 읽는다: 무엇이 이걸 깨뜨릴 수 있는가?

실패하면 테스트를 고치지 말고 코드를 고친다. skip 금지.
전부 통과한 뒤에만 커밋한다.
