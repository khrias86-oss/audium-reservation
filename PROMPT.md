# Claude Code 지시 프롬프트 — Audeum(오디움) 관람 예약 자동화 시스템

> 이 문서 전체를 Claude Code 세션에 그대로 붙여넣어 사용한다.
> 문서는 4부로 구성된다: **0. 컨텍스트/제약 → 1. 하네스 엔지니어링 → 2. 루프 엔지니어링 → 3. 제품 스펙(시나리오/UX) → 4. 마일스톤/수락기준**

---

## 0. 컨텍스트와 하드 제약

### 0.1 목표
`https://audeum.org/booking` 에서 **사용자가 지정한 날짜/회차**에 대해 **10분 주기로 잔여석을 확인**하고, **빈자리가 생기면 즉시 이메일로 알리고 자동으로 예약을 확정**하는 개인용 시스템을 만든다.

### 0.2 확정된 의사결정 (사용자 승인 완료)

| 항목 | 결정 |
|---|---|
| 실행 환경 | 무료 클라우드 (Vercel 우선 검토, 단 §0.4 제약 확인 필수) |
| 알림 채널 | 이메일 (`khrias86@gmail.com`) |
| 자동화 수위 | **완전 자동** — 감지 즉시 사람 개입 없이 예약 확정, 사후 통보 |
| 조작 UI | 웹 대시보드 (달력 기반 클릭 선택) |
| 폴링 주기 | 기본 10분 (설정 가능, §2.3의 적응형 주기 옵션 포함) |

### 0.3 도메인 사전 지식 (웹 검색으로 확보, **반드시 실측으로 재검증할 것**)
아래는 2차 출처(블로그/지역언론) 기반의 **가설**이다. 코드에 하드코딩하지 말고 §1.4 정찰 단계에서 실제 사이트로 검증한 뒤 `docs/site-contract.md`에 확정 기록한다.

- 운영일: **목·금·토** (일~수 휴관)
- 도슨트 회차: **10:00 / 11:00 / 13:30 / 14:30 / 15:30** (1일 5회차)
- 예약 오픈: **관람 2주 전 ~ 매주 화요일 14:00에 차주분 오픈** (두 설명이 상충 → 실측 필요)
- 입장료 무료, **1인 1매**, **본인 명의만 가능(대리예약 불가)**, 중복 예약 시 취소될 수 있음
- 예약 완료 시 **QR 티켓 이메일** 발송 (5분 내)
- 취소는 **예약 확인 이메일의 취소 링크**로만 가능 (전화 취소 불가)
- **취소표는 관람일 1~2일 전 및 당일 오전에 간헐적으로 발생** → 이 시간대가 폴링 가치가 가장 높은 구간
- 10:00 회차는 오르골 시연 포함으로 경쟁이 가장 치열

### 0.4 반드시 검증하고 넘어가야 할 인프라 제약 (선행 조사에서 확인됨)

**⚠️ Vercel Hobby 플랜의 Cron은 하루 1회가 최대다.** `*/10 * * * *` 같은 표현은 배포 시점에 실패한다. 또한 실행 시각도 "1시간 이내 보장"이라 10분 주기 폴링을 Vercel Cron만으로는 구현할 수 없다.

따라서 아키텍처를 **대시보드(Vercel)와 폴링 워커(별도)로 분리**한다. 워커 후보는 정찰 결과에 따라 갈린다:

- **경로 A — 사이트가 JSON API를 노출하는 경우 (권장):**
  브라우저 없이 HTTP 폴링이 가능하므로 **Cloudflare Workers Cron Triggers**(무료 플랜에서 분 단위 cron 지원, 하루 144회는 무료 한도의 극히 일부)가 최적. 콜드스타트/비용/안정성 모두 유리.
- **경로 B — 렌더링된 DOM을 읽어야만 하는 경우:**
  Playwright가 필요하므로 상시 실행 컨테이너가 필요. 후보를 이 순서로 실측 검증:
  1. **Oracle Cloud Always Free** ARM VM (영구 무료, 스펙 충분, Playwright 24/7 실행에 가장 적합)
  2. **Render 무료 티어** (2026년 기준 무료 티어 유지, 단 웹 서비스는 15분 유휴 시 슬립 → 워커 적합성 실측 필요)
  3. **GitHub Actions `schedule`** (최소 5분 주기이나 지연이 크고, private repo는 월 2,000분 한도라 10분 폴링에 부족. public repo는 무제한이나 개인정보 노출 리스크 → 최후 수단)

**첫 번째 작업은 정찰이고, 정찰 결과가 배포 경로를 결정한다.** 인프라를 먼저 고르지 말 것.

### 0.5 윤리·법적 가드레일 (타협 불가)

이 시스템은 **본인 1인의 무료 관람 예약**만을 위한 개인 도구다. 아래를 코드 레벨에서 강제한다.

1. `robots.txt`와 이용약관을 **가장 먼저** 확인하고 `docs/compliance.md`에 원문 인용과 함께 기록한다. 자동화를 명시적으로 금지한다면 **즉시 중단하고 사용자에게 보고**한다. 우회하지 말 것.
2. 폴링 주기는 **10분 미만으로 내려가지 않는다**(적응형 모드에서도 최소 5분 하한). 동시 요청 1개. 요청 간 최소 간격 강제.
3. `429`/`503`/`Retry-After` 수신 시 **지수 백오프**(1→2→4→8→16분, 상한 60분)하고 알림을 보낸다.
4. User-Agent를 위조하지 않는다. 봇 탐지 회피 기법(프록시 로테이션, IP 우회, CAPTCHA 자동 해제, 헤드리스 은닉 플러그인)을 **일절 사용하지 않는다**. CAPTCHA나 본인인증이 나타나면 **멈추고 사람에게 넘긴다**(§2.6).
5. **1인 1매 강제**: 예약 성공 시 해당 사용자의 모든 활성 감시를 자동 종료한다. 이미 유효한 예약이 있으면 새 예약을 시도하지 않는다(중복 예약은 사이트 정책상 취소 사유).
6. 사용자가 대시보드에서 명시적으로 선택한 날짜/회차 **밖으로는 절대 예약하지 않는다**.
7. 티켓 재판매·대리예약·다중 계정 기능은 만들지 않는다. 요청받아도 만들지 않는다.
8. 예약 성공 후 **가지 못하게 되면 즉시 취소**하도록 이메일과 대시보드에서 안내한다(노쇼는 향후 예약 제한 사유).

---

## 1. 하네스 엔지니어링 — 에이전트가 일할 환경부터 만든다

코드를 쓰기 전에 **네가 일할 환경**을 먼저 구축한다. 이것이 이 프로젝트의 1일차 작업이다.

### 1.1 `CLAUDE.md` 작성
저장소 루트에 다음을 담은 `CLAUDE.md`를 만든다.

- 프로젝트 한 줄 요약과 아키텍처 다이어그램(텍스트)
- **불변 규칙(NEVER 목록)**:
  - `DRY_RUN=false` 환경변수가 명시적으로 설정되지 않으면 **실제 예약 제출(POST) 금지**
  - 폴링 주기 5분 미만 금지
  - 봇 탐지 회피 코드 금지
  - `.env`, 쿠키/세션 파일, 예약자 개인정보(이름·연락처·이메일) **커밋 금지**
  - 실제 사이트를 대상으로 한 반복 테스트 금지 → 반드시 §1.5 픽스처 사용
- 개발 명령어: `pnpm dev`, `pnpm test`, `pnpm lint`, `pnpm typecheck`, `pnpm recon`, `pnpm dryrun`
- 셀렉터/엔드포인트는 **`src/adapters/audeum/contract.ts` 한 곳에만** 존재한다는 규칙

### 1.2 `.claude/settings.json` — 권한과 훅
- **허용 목록**: `pnpm *`, `git *`(push 제외 별도), `curl`(정찰용), `npx playwright *`
- **차단**: `rm -rf`, 시크릿 파일 접근, 프로덕션 배포 명령의 무확인 실행
- **훅**:
  - `SessionStart`: `pnpm install --frozen-lockfile` 자동 실행
  - `PostToolUse`(Edit|Write, `*.ts`/`*.tsx` 대상): `pnpm lint --fix && pnpm typecheck` 자동 실행 → 타입 에러를 다음 턴이 아니라 **즉시** 잡는다
  - `PreToolUse`(Bash): `git push` 전 시크릿 스캔(`gitleaks detect`) 실행, 검출 시 차단

### 1.3 슬래시 커맨드 (`.claude/commands/`)
반복 작업을 커맨드로 고정해 세션 간 재현성을 확보한다.

- `/recon` — §1.4 정찰 프로토콜 전체를 1회 실행하고 `docs/site-contract.md`를 갱신
- `/dryrun` — 실제 제출 직전까지 전 과정을 실행하고 페이로드·스크린샷만 남김
- `/contract-check` — 저장된 계약과 실제 사이트를 대조해 드리프트 검출
- `/verify` — lint + typecheck + unit + e2e(픽스처 기반) + 대시보드 스크린샷 캡처를 한 번에

### 1.4 정찰 프로토콜 (`/recon`) — **사이트 구조를 스스로 파악하는 절차**

⚠️ 이 프롬프트를 작성한 세션에서는 `audeum.org` 접근이 네트워크 정책으로 차단되어 **실측을 하지 못했다.** 따라서 셀렉터·엔드포인트는 이 문서에 하나도 하드코딩되어 있지 않다. 아래 절차를 **네가 직접 수행해서** 확정하라.

**단계 1 — 정적 표면 확인**
```
curl -sSI https://audeum.org/booking          # 상태코드, 서버, 쿠키, 리다이렉트
curl -sS  https://audeum.org/robots.txt       # 크롤링 정책 (§0.5)
curl -sS  https://audeum.org/sitemap.xml
curl -sS  https://audeum.org/booking -o raw.html
```
`raw.html`에서 다음을 찾는다: `__NEXT_DATA__`, `window.__NUXT__`, `__INITIAL_STATE__`, `<div id="root">`가 빈 껍데기인지, 인라인 JSON, 프레임워크 지문(Next.js/Nuxt/React/Vue/imweb/Wix/Cafe24 등).
→ **판정**: SSR(HTML에 슬롯 정보가 이미 있음) vs SPA(빈 껍데기 + XHR로 채움).

**단계 2 — 네트워크 트래픽 캡처 (핵심)**
Playwright로 페이지를 열고 모든 요청/응답을 기록한다.
```ts
page.on('request',  r => log.push({t:'req', method:r.method(), url:r.url(), body:r.postData()}));
page.on('response', async r => {
  if (r.request().resourceType() === 'xhr' || r.request().resourceType() === 'fetch') {
    log.push({t:'res', url:r.url(), status:r.status(), body: await r.text().catch(()=>null)});
  }
});
```
그리고 **사용자 행동을 하나씩 재현하며 트래픽을 diff한다**:
1. 페이지 최초 로드 → 어떤 API가 월별 가용 날짜를 주는가?
2. 달력에서 **날짜 A 클릭** → 어떤 요청이 새로 발생하는가? (= 슬롯 조회 API)
3. 달력에서 **날짜 B 클릭** → 요청 URL의 어떤 파라미터가 바뀌는가? (= 날짜 파라미터 형식 확정)
4. **매진된 날짜**와 **여석 있는 날짜**의 응답 JSON/DOM을 각각 저장해 **diff** → 매진 판별 필드 확정
5. 회차 선택 → 예약 폼 진입 → **제출 직전까지** 진행하며 폼 필드·hidden·CSRF 토큰·본인인증 iframe 유무 확인 (**제출은 하지 않는다**)

**단계 3 — 매진 판별 방식 확정**
아래 후보 중 **실제로 무엇인지**를 판정하고 우선순위를 기록한다. 여러 신호가 있으면 다중 신호로 교차 검증한다(하나만 믿지 말 것).
- API JSON 필드: `remain`, `remainCount`, `capacity`, `bookedCount`, `available`, `status: "SOLD_OUT"`
- DOM 클래스/속성: `.sold-out`, `.disabled`, `[aria-disabled="true"]`, `<button disabled>`
- 텍스트: "마감", "매진", "잔여 0", "예약마감", "신청불가"
- 요소의 존재/부재 자체 (버튼이 아예 렌더링되지 않음)

**단계 4 — 계약 문서화**
`docs/site-contract.md`에 다음을 표로 확정 기록한다. **이 문서가 코드보다 먼저다.**

| 항목 | 값 | 확인 방법 | 확인 일시 |
|---|---|---|---|
| 렌더링 방식 | SSR / SPA | | |
| 월별 가용일 조회 | METHOD + URL + 파라미터 + 응답 스키마 | | |
| 슬롯 조회 | METHOD + URL + 파라미터 + 응답 스키마 | | |
| 예약 제출 | METHOD + URL + 요청 바디 스키마 | | |
| 매진 판별 신호 | 1순위/2순위 | | |
| 회차 정의 | 실제 회차 시각 목록 | | |
| 운영일 규칙 | 요일/휴관 | | |
| 예약 오픈 규칙 | 시각/범위 | | |
| 인증 요구 | 없음 / 본인인증 / 로그인 | | |
| robots.txt 관련 조항 | 원문 인용 | | |
| Rate limit 징후 | 429 발생 여부·임계 | | |

**단계 5 — 어댑터 구현**
계약을 `src/adapters/audeum/contract.ts` **한 파일**로 옮긴다. 셀렉터 문자열과 URL은 이 파일 밖에 존재해서는 안 된다. 사이트가 바뀌면 이 파일 하나만 고치면 되도록 만든다.

### 1.5 픽스처 기반 테스트 하네스
정찰에서 캡처한 실제 응답을 `tests/fixtures/`에 저장한다. **최소 5종**:
`all-available.json`, `partially-sold.json`, `fully-sold-out.json`, `error-429.json`, `malformed-schema.json`

전 테스트는 이 픽스처로 오프라인 실행된다. **실제 사이트를 반복 호출하는 테스트는 금지**(§0.5-2). 실사이트 대상 e2e는 `pnpm test:live` 로 분리하고 수동 실행만 허용, 그리고 `DRY_RUN=true`를 강제한다.

### 1.6 서브에이전트 분업 (선택적, 규모가 커지면)
- `recon-agent` — 정찰과 계약 문서 갱신 전담 (읽기 전용 권한)
- `worker-agent` — 폴링 루프와 예약 실행
- `ui-agent` — 대시보드
각 에이전트에 `docs/site-contract.md`를 필수 입력으로 준다.

---

## 2. 루프 엔지니어링 — 두 개의 루프를 설계한다

### 2.1 루프 A: 런타임 폴링 루프 (제품의 심장)

**상태 기계**를 명시적으로 구현한다. 암묵적 플래그로 상태를 관리하지 말 것.

```
IDLE ──(감시 등록)──▶ WATCHING
WATCHING ──(tick: 여석 감지)──▶ DETECTED
DETECTED ──(즉시)──▶ CLAIMING ──(성공)──▶ BOOKED ──▶ (해당 사용자 전체 감시 STOPPED)
                              └─(실패)──▶ RETRY(최대 2회) ──▶ FAILED_NEEDS_HUMAN
WATCHING ──(연속 실패 5회)──▶ CIRCUIT_OPEN ──(30분)──▶ WATCHING
WATCHING ──(계약 드리프트)──▶ CONTRACT_BROKEN (알림 + 정지)
WATCHING ──(관람일 경과)──▶ EXPIRED
* 어느 상태에서든 ──(킬스위치)──▶ PAUSED
```

**틱(tick) 1회의 처리 순서** — 각 단계는 독립 함수로 분리하고 개별 테스트를 붙인다.
```
fetch → parse → normalize(Slot[]) → validate(schema) → diff(직전 스냅샷)
     → match(사용자 조건) → decide → act(예약) → persist → notify
```

**핵심 원칙**
1. **정규화 우선**: 사이트 응답을 즉시 내부 표준 타입 `Slot { date, time, capacity, remain, status, bookUrl }`로 변환한다. 이후 모든 로직은 이 타입만 안다. 사이트 변경의 파급을 어댑터 경계에서 차단한다.
2. **스키마 검증**: Zod로 응답을 검증한다. 검증 실패 = 조용한 실패가 아니라 **`CONTRACT_BROKEN` 전이 + 즉시 알림**. "빈자리를 못 찾은 것"과 "코드가 깨진 것"을 절대 혼동하지 않는다. — **이것이 이 시스템에서 가장 중요한 설계 결정이다.** 대부분의 예약봇은 사이트가 바뀐 줄 모르고 조용히 계속 실패한다.
3. **멱등성**: `bookings` 테이블에 `UNIQUE(userId, slotDate, slotTime)` 제약. 워커가 중복 실행되거나 재시작돼도 이중 예약이 발생하지 않는다.
4. **지터**: 10분 간격에 ±60초 랜덤 지터. 목적은 탐지 회피가 아니라 서버 부하 분산과 재시작 시 동기화 방지.
5. **타임아웃 예산**: 틱 전체 30초 상한. 초과 시 중단하고 다음 틱으로. 틱이 겹치지 않도록 뮤텍스(DB 어드바이저리 락).

### 2.2 서킷 브레이커와 데드맨 스위치
- **서킷 브레이커**: 연속 5회 실패 → `CIRCUIT_OPEN` 30분 + 이메일 알림 1회(스팸 방지).
- **데드맨 스위치**: 워커가 매 틱마다 `heartbeat` 타임스탬프를 기록. 대시보드는 **마지막 하트비트가 25분 이상 지나면 "감시 중단됨" 경고를 크게 표시**하고 이메일을 보낸다. → 사용자가 "돌아가고 있겠지"라고 착각하는 최악의 실패 모드를 막는다.
- **드리프트 감시**: `/contract-check`를 하루 1회 자동 실행해 계약 유효성을 점검.

### 2.3 적응형 폴링 (기본 10분 유지, 옵션)
§0.3에 따르면 취소표는 **관람일 1~2일 전과 당일 오전**에 집중된다. 사용자가 켤 수 있는 옵션으로 제공한다(기본 OFF, 최소 주기 5분 하한 준수).

| 구간 | 주기 |
|---|---|
| 관람일 7일 초과 | 30분 |
| 관람일 2~7일 전 | 10분 (기본) |
| 관람일 1일 전 ~ 당일 | 5분 |
| 예약 오픈 시각 ±10분 (정찰로 확정된 시각) | 5분 |

### 2.4 완전 자동 예약(CLAIMING) 실행 규칙
사용자가 **완전 자동**을 선택했으므로 감지 즉시 예약한다. 다만 다음을 코드로 강제한다.
1. **선순위 매칭**: 여러 슬롯이 동시에 열리면 사용자가 대시보드에서 지정한 **우선순위 순서**로 1개만 시도한다.
2. **1건 성공 = 전체 종료**: 성공 즉시 그 사용자의 모든 활성 감시를 `STOPPED`로 전이(§0.5-5).
3. **재시도 2회 한정**: 폼 제출 실패 시 최대 2회 재시도(2초, 5초 간격). 그 이상은 `FAILED_NEEDS_HUMAN` + **"지금 직접 예약하세요" 링크가 담긴 긴급 이메일**.
4. **전 과정 증거 보존**: 요청 페이로드, 응답, 각 단계 스크린샷을 `runs/<timestamp>/`에 저장. 실패 원인 규명과 사후 검증에 필수.
5. **DRY_RUN 게이트**: 제출 직전 `if (process.env.DRY_RUN !== 'false') { log payload; screenshot; return SIMULATED; }`

### 2.5 루프 B: 개발 루프 (Claude Code 자신의 작업 루프)

각 마일스톤은 **반드시** 이 루프를 완주한 뒤 다음으로 넘어간다. 코드를 쓰고 "됐을 것"이라고 넘기지 않는다.

```
계획 → 구현 → pnpm verify(lint+typecheck+test) → 실제 실행 관찰 → 증거 캡처 → 커밋
                                    ↑                                    │
                                    └──────── 실패 시 되돌아옴 ──────────┘
```

**"실제 실행 관찰"의 구체적 의미** (단위테스트 통과는 관찰이 아니다):
- 대시보드 작업 → Playwright로 브라우저를 띄워 **스크린샷을 찍고 그 이미지를 직접 확인**한다. 렌더링이 깨졌는지 눈으로 본다.
- 워커 작업 → 픽스처를 주입해 실제로 루프를 3틱 돌리고 상태 전이 로그를 확인한다.
- 알림 작업 → 실제로 테스트 메일을 발송하고 수신을 확인한다.
- 예약 작업 → `/dryrun`으로 제출 직전까지 실행하고 페이로드가 정확한지 스크린샷과 대조한다.

**실패 처리 규칙**: 테스트가 실패하면 테스트를 고치지 말고 코드를 고친다. 테스트를 skip/disable하지 않는다. 원인을 모르면 로그를 추가해 재현부터 한다.

### 2.6 사람에게 넘겨야 하는 경계 (Human-in-the-loop 예외)
완전 자동이지만, 아래는 **자동화하지 않고 즉시 사람에게 넘긴다**:
- CAPTCHA 등장
- 본인인증(휴대폰/PASS) 요구
- 로그인 세션 만료
- 계약 드리프트(`CONTRACT_BROKEN`)
- 응답에 예상치 못한 결제/동의 단계 출현

이때 이메일 제목은 `[조치필요]`로 시작하고, **원클릭으로 해당 예약 페이지로 이동하는 링크**를 최상단에 배치한다. 사용자가 30초 안에 손으로 끝낼 수 있게 한다.

---

## 3. 제품 스펙 — 사용자 시나리오와 UX

### 3.1 핵심 사용자 시나리오

**S1. 최초 설정 (1회, 3분 소요 목표)**
1. 대시보드 접속 → "예약자 정보" 입력(이름, 휴대폰, 이메일). 로컬/DB 암호화 저장, 1회 입력 후 재입력 없음.
2. 달력에서 원하는 날짜 클릭 → 그 날짜의 회차 칩(10:00/11:00/13:30/14:30/15:30)이 펼쳐짐 → 원하는 회차 클릭.
3. 여러 날짜·회차를 복수 선택 가능. 선택된 항목이 우측 "감시 목록"에 카드로 쌓임.
4. 카드를 드래그해 **우선순위** 정렬 (§2.4-1에서 사용).
5. "감시 시작" 버튼 → `WATCHING`.

**S2. 대기 중**
- 대시보드가 각 카드에 **다음 체크까지 남은 시간(카운트다운)**, **마지막 체크 시각**, **현재 잔여석**, **상태 배지**를 실시간 표시.
- 사용자는 창을 닫아도 된다(서버 워커가 계속 감시). 이 사실을 UI에 명시적으로 안내한다.

**S3. 빈자리 감지 → 자동 예약 성공 (핵심 경로)**
1. 워커가 여석 감지 → 즉시 예약 실행 (사용자 개입 없음)
2. 성공 시 이메일 발송: **`[예약완료] 오디움 3/14(토) 10:00 예약이 확정되었습니다`**
   - 본문 최상단: 날짜/회차/예약번호
   - "오디움에서 발송한 QR 티켓 메일을 5분 내 확인하세요" 안내
   - **취소 방법 안내**(가지 못하게 되면 즉시 취소 — 노쇼 시 향후 예약 제한, §0.5-8)
   - 대시보드 링크
3. 대시보드에 성공 배너 표시, 나머지 감시 전부 자동 종료(§2.4-2), 종료 사유를 화면에 명시.

**S4. 감지했으나 예약 실패**
- `[조치필요]` 이메일 + 대시보드 빨간 배너. **예약 페이지 직행 링크를 본문 첫 줄에** 배치.
- 감시는 계속 유지한다(다음 취소표를 노림).

**S5. 시스템 이상**
- 하트비트 끊김 / 계약 드리프트 / 서킷 오픈 → `[시스템경고]` 이메일 + 대시보드 상단 경고 바.

**S6. 중단**
- 전역 **킬스위치**(대시보드 우상단 항상 노출). 누르면 즉시 `PAUSED`, 진행 중 예약 시도도 중단.

### 3.2 UX 설계 원칙

**원칙 1 — "지금 무슨 일이 일어나고 있는지"를 한 화면에서 알 수 있어야 한다.**
가장 큰 실패는 "돌아가는 줄 알았는데 멈춰 있었다"이다. 화면 최상단에 **시스템 상태 바**를 고정한다: `🟢 감시 중 · 마지막 확인 2분 전 · 다음 확인 8분 후 · 감시 3건`. 하트비트가 끊기면 이 바가 빨간색으로 바뀐다.

**원칙 2 — 달력이 곧 컨트롤이다.**
별도 폼 없이 달력에서 직접 클릭해 감시를 켜고 끈다. 운영일(목·금·토)만 활성, 휴관일은 비활성 회색. 각 날짜 셀에 회차별 상태를 색으로 표시:
- 🟩 여석 있음 / 🟨 마감임박(잔여 1~2) / 🟥 매진 / ⬜ 미조회 / 🔵 감시 중(테두리 강조)

**원칙 3 — 대기 시간을 정직하게 보여준다.**
"확인 중..." 스피너 대신 **다음 확인까지 카운트다운**과 **최근 10회 확인 이력 타임라인**을 보여준다. 사용자가 시스템을 신뢰하려면 과거 동작이 보여야 한다.

**원칙 4 — 완전 자동의 불안을 UI로 상쇄한다.**
"감지 시 자동으로 예약합니다"를 감시 시작 버튼 옆에 상시 명시하고, **예약자 정보 미리보기**(무엇이 제출될지)를 접이식으로 항상 볼 수 있게 한다. 사용자가 자기가 무엇에 동의했는지 언제든 확인할 수 있어야 한다.

**원칙 5 — 모바일 우선.**
취소표는 사용자가 PC 앞에 없을 때 뜬다. 대시보드는 모바일에서 완전히 동작해야 하고, 이메일은 모바일에서 **제목만 봐도 결과를 알 수 있어야** 한다(`[예약완료]`/`[조치필요]`/`[시스템경고]` 접두사).

### 3.3 이메일 설계
- 발송: Resend 또는 Gmail SMTP(앱 비밀번호). 무료 한도 확인 후 선택.
- 제목 규격: `[상태] 오디움 M/D(요일) HH:MM — 한 줄 요약`
- **알림 중복 억제**: 같은 슬롯에 대해 동일 상태 알림은 1시간에 1회만. 시스템 경고는 서킷당 1회.
- 모든 메일 하단에 **킬스위치 링크**(원클릭 전체 중지) 포함.

### 3.4 기술 스택 제안 (정찰 결과에 따라 조정)
- 대시보드: **Next.js(App Router) on Vercel** — 사용자가 선택한 웹 대시보드
- DB/상태: **Neon 또는 Supabase 무료 Postgres** (Vercel과 워커가 공유하는 단일 진실 원천)
- 워커: **경로 A → Cloudflare Workers Cron** / **경로 B → Oracle Always Free VM + Node + Playwright + PM2** (§0.4)
- 검증: Zod / 테스트: Vitest + Playwright
- 시크릿: Vercel 환경변수 + 워커 플랫폼 시크릿. 저장소에는 `.env.example`만.

---

## 4. 마일스톤과 수락 기준

각 마일스톤 끝에서 §2.5 개발 루프를 완주하고 **커밋**한다. 중간에 다음 마일스톤으로 건너뛰지 않는다.

| # | 마일스톤 | 수락 기준 (이게 안 되면 통과 아님) |
|---|---|---|
| M0 | 하네스 구축 | `CLAUDE.md`, `.claude/settings.json`, 슬래시 커맨드 4종, `pnpm verify` 동작 |
| M1 | **정찰 완료** | `docs/site-contract.md`의 모든 표 항목이 "미확인" 없이 채워짐 + `docs/compliance.md`에 robots.txt/약관 인용. 픽스처 5종 확보 |
| M2 | 어댑터 + 파서 | 픽스처 5종 전부에 대해 `Slot[]` 정규화 테스트 통과. 스키마 깨진 픽스처에서 `CONTRACT_BROKEN` 발생 확인 |
| M3 | 폴링 루프 | 상태 기계 전이 전부 단위 테스트. 3틱 실제 실행 로그 확인. 서킷 브레이커·하트비트 동작 확인 |
| M4 | 이메일 알림 | 실제 수신 확인(스크린샷). 중복 억제 동작 확인 |
| M5 | 대시보드 | 달력 클릭→감시 등록→상태 표시 전 과정 Playwright 스크린샷으로 확인. 모바일 뷰포트 확인 |
| M6 | 자동 예약 | `/dryrun`으로 제출 페이로드 정확성 검증. 멱등성(중복 실행 시 1건만) 테스트 통과. **실제 제출은 사용자 승인 후 1회만** |
| M7 | 배포 | 대시보드 Vercel 배포 + 워커 24시간 무중단 확인. 하트비트 24시간 연속 기록 확인 |

### 4.1 작업 방식
- 각 마일스톤 시작 전 **계획을 먼저 제시**하고, 불확실하거나 사용자 결정이 필요한 지점은 **그때 질문**한다(추측으로 진행하지 않는다).
- 정찰(M1) 결과가 §0.3의 가설과 다르면 **가설을 버리고 실측을 따른다.** 실측 내용을 사용자에게 보고한다.
- 커밋 메시지는 `feat(worker): ...` 형식. 마일스톤 완료 시 그 증거(스크린샷 경로, 테스트 결과)를 커밋 본문에 남긴다.

### 4.2 최종 산출물
1. 동작하는 대시보드 URL
2. 24시간 무중단 워커
3. `docs/site-contract.md` — 사이트 구조 실측 문서
4. `docs/compliance.md` — 준수 사항 문서
5. `docs/runbook.md` — 장애 대응 절차(감시가 멈췄을 때, 사이트가 바뀌었을 때, 예약이 실패했을 때 각각 무엇을 하는가)
6. `README.md` — 로컬 실행 및 재배포 방법

---

## 5. 시작 지시

**M0(하네스)부터 시작하라. 그다음 M1(정찰)을 수행하고, 정찰 결과를 사용자에게 보고한 뒤 배포 경로(경로 A/B)를 확정하라.** 정찰 전에 인프라를 고르거나 셀렉터를 추측해 코드를 쓰지 말 것.

`robots.txt` 또는 이용약관이 자동화를 금지한다면 **즉시 중단하고 보고하라.**
